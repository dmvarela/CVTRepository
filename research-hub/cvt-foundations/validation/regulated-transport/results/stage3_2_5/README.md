# Stage 3.2.5 closure outputs

Status: final test not authorized.

Files:

- `authorization_decision.json` records the formal decision to keep the final test ungenerated and unopened.
- `capture_proxy_sensitivity_refit.json` fairly refits the locked `CVT-2-minimum` scorer under the primary micro-probe, 2.5x-dose micro-probe, and direct structural capture proxy.

Main conclusion: the dose sensitivity is effectively unchanged, while the direct structural composite remains worse after fair calibration (`Brier = 0.029099` versus primary `0.027566`). This does not rescue the locked Stage 3.2 result.

The final test partition remains ungenerated and unopened.
